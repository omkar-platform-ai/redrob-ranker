# Secrets
.env

# Pre-computed artifacts (large, machine-specific)
data/index/*
# …but pin the tiny LLM-parsed JD so Stage-3 reproduction uses the exact JD parse
# (derived only from the public job_description.txt — no candidate data, no secrets).
!data/index/parsed_jd.json
sample_index/
*.faiss
*.npy

# Dataset files (provided by hackathon, not ours to publish)
data/candidates.jsonl
data/candidates.jsonl.gz
data/*.csv

# Submission output (generate locally via rank.py)
submission.csv

# macOS
.DS_Store

# Python
__pycache__/
*.pyc
*.pyo
*.egg-info/
.venv/
venv/

# Tools
.pytest_cache/
.ruff_cache/

# Claude
.claude/
headroom_memory.db
.serena
redrob-hackathon-dev.skill
VALIDATION_REPORT.md